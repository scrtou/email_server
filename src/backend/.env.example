# 服务器配置
SERVER_PORT=5555

# Docker端口配置 (用于生产环境部署)
FRONTEND_PORT=80    # 前端端口 (生产环境)
BACKEND_PORT=5555   # 后端端口

# 数据库配置
SQLITE_FILE=./gorm.db

# JWT配置
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24

# LinuxDo OAuth2 配置
# 请到 https://connect.linux.do 申请应用获取以下信息
LINUXDO_CLIENT_ID=your_client_id_here
LINUXDO_CLIENT_SECRET=your_client_secret_here
LINUXDO_REDIRECT_URI=http://localhost:5555/api/v1/auth/oauth2/linuxdo/callback
LINUXDO_AUTH_URL=https://connect.linux.do/oauth2/authorize
LINUXDO_TOKEN_URL=https://connect.linux.do/oauth2/token
LINUXDO_USER_INFO_URL=https://connect.linux.do/api/user

# 前端配置
FRONTEND_BASE_URL=http://localhost:8080

# ========== 生产环境部署配置说明 ==========
# 部署到服务器时，请创建 .env.production 文件并修改以下配置：
#
# 1. JWT密钥 - 必须修改为强密钥
# JWT_SECRET=your-production-super-secret-jwt-key-at-least-32-characters-long
#
# 2. 数据库路径 - 使用容器内持久化路径
# SQLITE_FILE=/data/database.db
#
# 3. OAuth2回调地址 - 修改为您的域名
# LINUXDO_REDIRECT_URI=https://yourdomain.com/api/v1/auth/oauth2/linuxdo/callback
#
# 4. 如果使用HTTPS，还需要配置：
# SECURE_COOKIES=true
# CORS_ORIGINS=https://yourdomain.com
